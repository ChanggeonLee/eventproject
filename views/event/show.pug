extends ../layout

block content
    .container
        h1.main-title= event.title

        .event.show
            include _eventshow

        .buttons
            button.btn.btn-primary #[i.fa.fa-thumbs-up] Attendance
            if currentUser && event.author.id == currentUser.id
            | 
            a.btn.btn-outline-secondary(href=`/event/${event.id}/edit`) Edit
            | 
            a.btn.btn-outline-danger.need-confirm-btn(href=`/event/${event.id}?_method=delete`) Delete
      
    //- if event.numAnswers > 0
    //-     hr
    //-     h3 #{event.numAnswers} Answer(s)
    //-     .answers.show
    //-         each answer in answers
    //-         include _answer

    //- form(action=`/event/${event.id}/answers`, method='POST')
    //-     .form-group
    //-         label(for='answer') Your Answer
    //-         textarea.form-control(name="content", rows="5")
    //-     button.btn.btn-primary(type="submit") Post Your Answer

    //- script(src='/javascripts/question_client.js')
    script(src='/javascripts/event_client.js')

