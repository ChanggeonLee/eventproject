extends ../layout

block content
    .container
        form.col-10.mx-auto(action=`/event/${event.id}?_method=PUT`,method='POST')          
            include _form
            button.btn.registerbutton.btn-color(type="submit") Change Your Event

                
